# 全文合并说明

2026-09-13 更新。当前全文 28 页，第四问独立修订稿 7 页，均已重新编译与检查。`论文初稿.tex/pdf` 与 `../templates/论文模版.tex/pdf` 同步。

## 本次合入

第四问在原 `第四问_M32` 目录更新，沿用 `q4-m32-final.pdf/md`。按文献适配 CG、简洁旧版 M07、M32 组织问题解决过程，给出各 30 场官方演练、30 组本地同场诊断、同场路径对照和 3 次正式测验。保留 6 个编号公式、4 幅图和 3 张表。全部证据与冻结策略见 `第四问_M32/evidence`、`strategies`；图像由 `redraw_figures.py` 重绘。

全文章节使用全局编号。第四问中的正、负观测集合改记为 S 的正负上标（LaTeX 为 `\mathcal S^+`、`\mathcal S^-`），避免与前文 P、Q 冲突；谨慎策略风险参数加 c 下标，避免与第二问示向角混淆。参考文献去重并补入赵倩倩等的定向源文献。模型检验中的旧演练表述同步更新。

第一、二问正文逐字保留，第三问章节文件哈希保持不变；保留队友最新图表。未重新开展模拟器实验，也未改变采用的 M32 策略。正式测验缺少实际源总数，仅报告已核验清除数。M32 对 M07 的独立官方差异区间跨零，不宣称稳定领先。

## 编译与文件

解压源码包后，在 `梳理` 目录执行 `tectonic 编译入口.tex`，或两次 `xelatex 编译入口.tex`；输出 `编译入口.pdf` 即发布排版。该入口指定 TeX 自带 Fandol 字体，避免部分 Tectonic 环境无法加载 Windows 系统字体。原主文件仍保留跨平台自动字体选择。

压缩包保持仓库相对目录，包含主文件、章节、实际引用的第一二三四问图表及第三四问复现材料。请勿只复制主 tex 文件。模板目录使用同一份章节和图表路径。完整核验见 `全文合并核验.json`。

旧合并稿、旧页数与历史策略通过 Git 历史及已有合并记录查阅。
