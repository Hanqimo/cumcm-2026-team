# 四问初版全文合并说明

2026-09-13。用户要求将已完成的第三、四问上传至本目录，并与 GitHub 最新一、二问及其他部分合成全文，摘要暂不处理。

## 阅读入口

- [论文初稿.pdf](论文初稿.pdf)：合并后的 26 页全文，9 幅图、7 个表环境（包括全局符号表）、50 个编号公式。
- [论文初稿.tex](论文初稿.tex)：全文主文件；从本目录编译。
- [全文初版_源码与图表.zip](全文初版_源码与图表.zip)：完整编译包，含全文 PDF、主文件、章节源码及所有实际引用图件。
- [第三问独立稿](第三问_v6/q3-v6-final.pdf)：冻结 v6。
- [第四问独立稿](第四问_M32/q4-m32-final.pdf)：冻结 M32 joint。

## 来源及合并范围

1. 最新一、二问及前置内容采用 `templates/论文模版.tex`，对应用户指定的 `templates/论文模版.pdf`，读取时 GitHub main 为 `383289477e546fcec9cd2c9d9d0c73f58a7fa426`。一、二问章节的正文、公式及图件引用逐字保留，不以本地旧稿替换。
2. 第三问采用本地 `q3-v6-final.tex`，第四问采用本地 `q4-m32-final.tex`。两个独立稿目录保存原完成版本；全文调用 `全文章节/问题三.tex` 与 `全文章节/问题四.tex`。
3. 补齐原模板为空的“问题四分析”“模型检验”“模型评价与推广”，仅归纳已有方法和证据，没有新增实验或填造结果。
4. 保留论文标题，本次不撰写摘要与关键词，也不生成空白摘要页。后续撰写摘要时可恢复摘要页。
5. 图、表、公式按全文连续编号，参考文献各保留一条；文献 03、05 在当前总参考文献中对应 [4]、[5]。

## 符号统一

- 沿用全局 `g` 表示真实未知源位置；第四问多源下标改为 `g_j`，一般候选位置仍用 `x`。
- 全局 `P` 表示纯测角交会区域，第二问 `Q` 表示检测点搜索范围。因此第四问正、负观测位置集合改为花体 `S^+`、`S^-`，被迫背向集合相应写作 `S^-_B`。
- 全局 `alpha=pi/180` 始终为题设误差界；第四问的 1.0051 度仅作为几何实现的数值外扩角度，不重定义 alpha。
- 第四问一级标题统一为“问题四的模型与求解”。以上均为合稿的记号或标题修改，不改变策略、参数或实验结果。
- 模型假设末段明确第四问工作先验的用途，几何排除与可靠清除不依赖规划概率近似。

## 编译与核验

在 `梳理` 目录执行：

```sh
tectonic 论文初稿.tex
```

也可使用配齐模板依赖的 XeLaTeX 编译两遍。全文采用 TeX Live 自带 Fandol 中文字体，并修正原模板可移植字体分支中的不兼容 cmap 选项；原 `templates` 文件不改。

已核对全部图片存在、标签无重名、文献键无重复、引用已解析，并渲染检查 26 页。编译无文字溢出、缺字及未解析引用。来源哈希与一、二问逐字一致性见 [全文合并核验.json](全文合并核验.json)。本次只上传论文、图件及必要说明，不上传本地认证配置或原始交互请求。

全文目前仍为初版：两问的场景结果均明确为官方演练，未写入尚无核验记录的三次正式测试成绩。原论文各项结果的证据范围予以保留。
