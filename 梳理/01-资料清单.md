# B题 资料清单

> 本文档分两大部分：**第一部分 · 问题1**（原有内容）与 **第二部分 · 问题2**（本次新增）。
>
> 路径说明：默认以较新的 **`正式题目工作区/B题/`** 为基准。根目录下的 `contest/` 是 git 主线上的旧同步副本，缺少 v02 补充，其余 v01 内容相同。下文对两者并存的情况会注明。

---

# 第一部分 · 问题1

## 一、问题1 核心研究目录 `contest/q1/`

### 1. 状态与决策类（在 `contest/q1/` 下）

| 文件 | 作用 |
|---|---|
| `state.md` | **问题1研究状态总览**：当前阶段、候选模型（Q1-M01-v01 + v02 补充）、采用状态（"未确定"）、已完成验证（R003 共 260 项）、当前假设、未决风险、下一步、复现命令。写论文/汇报时先看这个。 |
| `adopted.md` | **正式采用方案唯一入口**。当前所有字段均为"—/未采用"，说明问题1尚无团队正式定稿结论，一切成果仍属"研究分支推荐"。 |
| `research-log.md` | **研究日志**：R001（SLSQP 线搜索退出未通过）→ R002（修正凸范数约束后通过）→ R003（修正图形裁切后通过）→ v02 补充（最近保证清除点，200 例独立核验）。记录了试错与被否决的方案。 |
| `decisions.md` | **决策记录**：4 条关键选择及理由——①用正向角域交集描述不确定位置；②不给纯角域加任意有限盒子（避免掩盖无界）；③同直径圆判定与清除半径分别处理（D/2 不等于包围半径）；④先用可审查枚举算法而非快速随机增量算法。 |
| `comparison.md` | **候选模型比较表**：M01 有界角域（推荐）vs 中心线交点（错误简化）vs 高斯误差椭圆（暂不采用）。 |
| `README.md` | q1 目录说明与入口。 |

### 2. 跨模型公共接口（`contest/q1/common/`）

| 文件 | 作用 |
|---|---|
| `v1-handoff.md` | **第一版对外接口契约**：列出 6 个可复用函数（`observations_halfplanes`、`halfplane_region`、`diameter`、`enclosing_circle`、`summarize_region`、`outer_disk`/`clip_halfplanes`）及各自"必须保留的条件"，是向 Q2/Q3/Q4 传递结果、避免误用的关键文档。核心规则：`D≤40` 不保证可清除，`R≤20` 才给充要条件。 |

### 3. 模型 `contest/q1/models/m01-bounded-bearing/`

| 文件 | 作用 |
|---|---|
| `README.md` | 模型说明、依赖（Python 3.10 / NumPy / SciPy / Matplotlib）、复现命令、R001–R003 运行差异说明。 |
| `formulations/v01.md` | ★ **问题1完整数学表述与证明（最重要文件）**：正向角域定义、半平面表示（Q1-1/Q1-2）、区域状态与顶点算法（A01）、直径顶点公式及证明（Q1-3）、同直径圆判定（Q1-4）、38 米等边三角形反例（Q1-5）、最小包围圆计算、清除判据（Q1-6/Q1-7）、数值与舍入边界、验证说明。写论文的公式与证明直接取自此文件。 |
| `formulations/v02-clearance-projection.md` | ★ v02 补充：**最近的保证清除位置**——把清除位置集合写成 `K_ρ = ∩_i B(v_i, ρ)`，求从机器人当前位置到该凸集的欧氏投影，用圆弧径向投影 + 两圆交点枚举求解，是第三问行动问题的几何接口。 |
| `src/bearing_geometry.py` | ★ 核心几何实现：`wedge`/`observations_halfplanes`（角域→半平面）、`halfplane_region`（LP 判断空集/无界 + 交点枚举顶点）、`diameter`、`enclosing_circle`、`summarize_region`、`clip_polygon`/`clip_halfplanes`、`outer_disk`/`disk_halfplanes`。 |
| `src/clearance_geometry.py` | v02 的实现：`nearest_clear_point`（对等半径清除圆盘交集的最近点投影）。 |
| `verification/verify_q1.py` | 验证脚本（生成 R00X 运行，含 260 项检查：3500 个 atan2 判断、60 组多站一致观测、解析特例、空集/无界/点/线段退化、平移旋转不变、新增观测单调性）。 |

### 4. 运行结果 `contest/q1/models/m01-bounded-bearing/runs/`

| 文件 | 作用 |
|---|---|
| `R003/` | ★ **当前引用运行（通过）**：`verification.json`（260 项检查明细）、`manifest.json`/`manifest.yaml`（运行配置与哈希）、`q1_geometry.png`/`q1_geometry.svg`（反例与正交观测示意图）、`orthogonal_example.json`（正交观测特例）、`counterexample.json`（38 米等边三角形反例的测点与示向度数据）、`source_snapshot/`（源码快照）。 |
| `R001/` | 首次运行（独立 SLSQP 圆半径核验因平方差形式线搜索退出，**未通过**），保留 `failure.json`。 |
| `R002/` | 修正独立验证的范数约束后通过（260 项）。 |

## 二、全题共享约定 `contest/global/`

| 文件 | 作用 |
|---|---|
| `assumptions.md` | **全局假设 A01–A06**：有界误差集合（不指定概率分布）、纯角域多边形与物理先验分别命名、`r ≤ R` 关系、初始域省略 5 米内孔、`h=20` 与预算示例、1.005° 工程敏感性。写论文"模型假设"一节直接取材。 |
| `symbols.md` | **全局符号表**：`g`、`s_i, θ_i`、`ε`、`W_i`、`P`、`D(P)`、`R(P)`、`R`（接收半径）、`Ω`（1800 米圆域）等。保证全队符号统一。 |
| `dependencies.md` | **小问依赖表**：问题1向问题2/3/4 传递"半平面定位区域、D、R 及清除判据"，并注明传递时需保持一致的前提。 |
| `decisions.md` | 全局决策记录（当前为空）。 |
| `accepted-results/README.md` | 正式结果的发布规范（当前问题1尚未有内容发布到此）。 |
| `problem/B-v1/B题.pdf` | ★ **题目原文**（4 页）：B 题"无线电干扰源的快速自动定位与清除"，问题1 在第 1 页，附录1/2 给出干扰源特征与测向机原理（误差界 ±1°、示向度定义、20 米光学清除等）。 |
| `problem/B-v1/附件/附件1.docx`、`附件2.docx` | 官方附件（模拟器 HTTP+JSON 通信协议，主要用于问题3/4，问题1不直接使用）。 |

## 三、论文准备 `contest/paper/`

| 文件 | 作用 |
|---|---|
| `notes/q12-v1-writing-handoff.md` | ★ **前两问写作交接**：给出建议写作顺序、哪些材料可写入正文、每个材料必须保留的限定、定稿前需决定的问题、不得写成已完成的事项。 |
| `notes/q12-v1-literature.md` | ★ **文献依据与自主推导界限**：7 篇参考文献（L01 Tokekar2013、L02 Gholami2015、L03 VanderHook2014、L04 Yao2024、L06 Sheng2016、L07 Bai2009、L11 Welzl1991）各用了什么、不可直接移植什么，以及 6 条"本轮独立推导、需作为本题工作呈现"的内容。 |
| `notes/q12-v1-provenance.json` | 原始输入与 7 篇核心文献 PDF 的来源与 SHA-256（追溯用）。 |
| `notes/q12-v1-result-index.json`、`q12-v1-file-inventory.json`、`q12-v1-ai-record.md` | 结果索引、文件清单、AI 使用记录。 |
| `bibliography/q12-v1.bib` | 前两问参考文献的 BibTeX。 |
| `tables/q12-v1-results.md` | 自动生成的数值比较表（Q1/Q2，含原点首次 0° 的四种预算示例）。 |
| `notes/build_q12_v1_record.py` | 生成表格与文献索引的脚本。 |

## 四、顶层研究导航

| 文件 | 作用 |
|---|---|
| `正式题目工作区/B题/README.md` | ★ B 题四问最新成果汇总入口（含各问最新思路/结果与阅读顺序）。 |
| `正式题目工作区/B题/四问思路与结果汇总.md` | ★ 四问思路、结果与相互依赖的精炼汇总（问题1部分在开头）。 |
| `RESEARCH_Q12_V1.md`（根目录） | ★ 前两问第一版建模导读：从哪里读、关键结果、验证与复现、尚需保留的边界。 |
| `RESEARCH_B_PROGRESS.md`（根目录） | B 题第 1–3 问研究成果导航。 |

## 五、参考思路与历年同类题 `正式题目工作区/B题/参考思路/历年同类题研读/`

| 文件 | 作用 |
|---|---|
| `README.md` | 历年同类题导航：今年最重要的参照是 **2022 B 纯方位定位、2023 B 覆盖测线、2020 B 在线决策**；12 篇优秀论文按用途组织（P01–P12）。 |
| `建模与写作要领.md` | ★ 建模与写作要领（33 KB，建议精读）。 |
| `写作骨架与实验清单.md` | ★ **写作骨架与实验清单**：四问交付对齐、章节职责、核心模块论述骨架、最有价值图表组合、实验设计清单（正确性/表现/保证）、摘要骨架、交稿前自查。 |
| `优秀论文研读卡.md` | 12 篇论文的逐篇研读卡。 |
| `论文清单与核验.json` | 论文清单、页数、阅读范围与哈希核验。 |
| `第三四问相似子问题与优秀论文处理方法.md` | 第三/四问相似子问题处理（问题1较少用到）。 |

## 六、历年 B 题与题面副本

| 文件 | 作用 |
|---|---|
| `resources/problems/B/README.md` | 2021–2025 历年 B 题索引（含 2022 纯方位无源定位等，作为问题1同类题背景）。 |
| `CUMCM2026Problems/B题/` | 与 `contest/global/problem/B-v1/` **内容完全相同的题面副本**（B题.pdf + 附件1/2.docx，文件大小一致），二选一即可。 |

## 七、两份工作区的对应关系（去重提示）

| 根目录（旧同步，git 主线） | `正式题目工作区/B题/`（较新，推荐） |
|---|---|
| `contest/q1/…`（无 v02、无 clearance_geometry.py） | `contest/q1/…`（含 v02 与 clearance_geometry.py） |
| `contest/global/…` | `contest/global/…`（内容一致） |
| `contest/paper/…` | `contest/paper/…`（内容一致） |
| — | 新增 `参考思路/`、`参考论文/`、`完整运行归档/`、`四问思路与结果汇总.md` |

> 结论：**问题1 相关文件请优先看 `正式题目工作区/B题/contest/q1/`**；若只需 git 上的正式同步版本，则看根目录 `contest/q1/`。

---

# 第二部分 · 问题2

## 一、问题2 核心研究目录 `contest/q2/`

### 1. 状态与决策类（在 `contest/q2/` 下）

| 文件 | 作用 |
|---|---|
| `state.md` | **问题2研究状态总览**：当前阶段（G0–G5 完成第一版 + v02 补充）、候选模型（Q2-M01-v01 + v02-region-reception）、采用状态（"未确定"）、已完成验证（R002 共 1305 项检查、1100 条方向工况；V02 另 40 例独立核验）、当前假设、未决风险、下一步、复现命令。 |
| `adopted.md` | **正式采用方案唯一入口**。当前所有字段均为"—/未采用"，问题2 尚无团队正式定稿。 |
| `research-log.md` | **研究日志**：复核 L03/L04/L06/L07 → 推导三圆盘区域 → R001（粗细分辨率改变排名，750米点略逊45°基线）→ R002（最终分辨率重排）→ V02（独立核验）→ 2026-09-11 v02 补充。 |
| `decisions.md` | **决策记录**：5 条关键选择——①先保证可接收再优化定位半径；②用预算曲线表达精度-移动成本；③连续读数分箱外包；④保留45°低成本基线；⑤不把全向接收证明移植到定向源。 |
| `comparison.md` | **候选模型比较表**：M01 接收约束 minimax（推荐）vs 45°斜向基线 vs 沿方向前进 vs 单纯横移（否决，有失联反例）vs 概率平均信息增益（暂不采用）。 |
| `README.md` | q2 目录说明与入口。 |

### 2. 跨模型公共接口（`contest/q2/common/`）

| 文件 | 作用 |
|---|---|
| `v1-handoff.md` | **第一版对外接口契约**：`planner.plan_next_observation` 等 7 个接口及边界；输入（首次位置 s、读数 θ、误差界 ε、预算 B、横向分离 h，**不能传入隐藏真实目标距离**）、输出（第二点 q、候选域余量、最坏后验半径上界 U、分箱宽度、预计耗时）。 |

### 3. 模型 `contest/q2/models/m01-reception-minimax/`

| 文件 | 作用 |
|---|---|
| `README.md` | 模型说明、复现命令（`python -X utf8 contest/q2/models/m01-reception-minimax/src/run_q2.py --run-id R003`）、当前完整运行约 17 秒。 |
| `formulations/v01.md` | ★ **问题2完整数学表述与证明（最重要文件）**：已知量/评价标准（Q2-1/Q2-2）、三圆盘候选域 K 及保证接收证明（Q2-3~Q2-5）、退化排除 C(B,h)（Q2-6/Q2-7）、45°基线（Q2-8）、决策模型 F(q)（Q2-9~Q2-11）、线性近似 R_lin（Q2-12）、数值算法与读数分箱外包（Q2-13）、四预算数值结果。写论文公式与证明直接取自此文件。 |
| `formulations/v02-region-reception.md` | ★ v02 补充：**结合当前定位区域的接收判定**——给定当前凸多边形外包 P、首次正观测位置 s、拟议测点 q，判定是否对所有剩余位置保证接收。接口 `reception_violation`，有限枚举（顶点 + 边圆交点 + 反径点）求最大违约量。第三问 M03 的候选接口。 |
| `src/second_point.py` | ★ 核心几何实现：`first_region`（首次凸外包）、`candidate_margins`/`in_candidate`（第二点安全与非退化检查）、`posterior`（第二次读数后更新）、`worst_radius_upper`（连续读数分箱外包上界）、`to_global`/`to_local`、`linearized_radius`（线性近似）。 |
| `src/planner.py` | 通用选点入口 `plan_next_observation(sensor, theta, budget)`，双侧有限网格搜索。 |
| `src/reception_region.py` | v02 实现：`reception_violation`（对凸多边形区域的精确接收违约判定）。 |
| `src/run_q2.py` | 运行及验证脚本（生成 R00X 运行）。 |
| `verification/independent_check.py` | V02 独立核验（另一几何构造 + LP 支持函数核验 40 例）。 |
| `verification/check_planner.py` | V03 通用入口边界场景检查（原点、圆域边缘、非轴向读数、预算过小）。 |

### 4. 运行结果 `runs/`

| 文件 | 作用 |
|---|---|
| `R002/` | ★ **当前引用运行（通过）**：`selected_points.csv`（四预算推荐点表）、`verification.json`（1305 项检查）、`baselines.csv`、`coarse_candidates.csv`/`refined_candidates.csv`、`q2_candidate_and_tradeoff.png/.svg`（候选区域与预算权衡图）、`q2_range_stress.png/.svg`（真实距离压力检查）、`worst_enclosure_B*.json`、`source_snapshot/`。 |
| `R001/` | 首次运行（粗精度排序导致 750 米预算入选点略逊 45° 基线的发现），保留。 |

### 5. 独立核验 `verification/`

| 文件 | 作用 |
|---|---|
| `V02/result.json` | 40 例独立几何构造 + LP 支持函数核验（与逐边裁剪后验半径一致）。 |
| `V03/results.json` | 通用入口 `plan_next_observation` 的边界场景检查。 |

## 二、问题2 相关论文与写作材料（`contest/paper/`）

| 文件 | 作用 |
|---|---|
| `sections/q2.tex` | ★ **问题2论文正文**（与 v01 内容一致的 LaTeX 版）。 |
| `figures/q2-candidate-tradeoff.png`、`q2-range-stress.png` | 问题2两张图（候选区域+预算权衡、真实距离压力）。 |
| `tables/q12-v1-results.md` | 前两问数值表（含问题2四预算推荐点）。 |
| `notes/q12-v1-writing-handoff.md` | 前两问写作交接（问题2 材料在 Q2-1~Q2-13 行）。 |
| `notes/q12-v1-literature.md` | 前两问文献依据与自主推导界限（L01/L03/L04/L06/L07/L11 在问题2 的使用）。 |
| `bibliography/q12-v1.bib` | 前两问参考文献 BibTeX。 |

## 三、顶层研究导航

| 文件 | 作用 |
|---|---|
| `正式题目工作区/B题/README.md` | B 题四问最新成果汇总入口（问题2 行）。 |
| `正式题目工作区/B题/四问思路与结果汇总.md` | ★ 四问汇总（问题2 部分给出四预算推荐点表）。 |
| `RESEARCH_Q12_V1.md`（根目录） | 前两问第一版建模导读。 |
| `RESEARCH_B_PROGRESS.md`（根目录） | B 题第 1–3 问研究成果导航。 |

## 四、两份工作区的对应关系（去重提示）

| 根目录（旧同步，git 主线） | `正式题目工作区/B题/`（较新，推荐） |
|---|---|
| `contest/q2/…`（无 v02、无 reception_region.py） | `contest/q2/…`（含 v02 与 reception_region.py） |
| `contest/global/…`、`contest/paper/…` | 内容一致 |

> 结论：**问题2 相关文件请优先看 `正式题目工作区/B题/contest/q2/`**；若只需 git 上的正式同步版本，则看根目录 `contest/q2/`。
