# 第四问 M32 修订论文

2026-09-13。7 页，包含参考文献；6 个编号公式、4 幅图、3 张表。本目录已在原文件位置更新；旧版可通过 Git 历史查阅。

## 交付内容

- `q4-m32-final.pdf`：修订后正式阅读版。
- `main.tex`、`chapter.tex`、`data-numbers.tex`：独立编译源；`chapter-resolved.tex` 已把统计数值写入正文，适合合并到总稿。
- `q4-m32-final.md`：可编辑阅读文本，图表与六个公式完整保留。
- `figures/`：4 图各有 PDF、SVG、400 dpi PNG。第 4 图含三条真实执行路径和清除进度。
- `evidence/`：90 场官方演练明细、3 场正式测验、30 场同场本地诊断、选择规则、哈希与校核。
- `strategies/`：实际官方演练冻结的 CG、M07、M32 数学策略代码，未包含认证配置。
- `redraw_figures.py`：只用提取数据重绘四图，不连接 API、不运行模拟器。

## 本次具体改动

1. 补入四篇学术论文的可迁移思想和使用边界，按“文献适配 CG → 简洁旧版 M07 → M32”解释问题解决过程。
2. 新实现 CG 并完成 30 次官方演练（30/30 全清）；M07 和 M32 各复用既有 30 场。
3. CG、M07、M32 官方均值为 651.83、496.70、458.37 s/源；相对 CG 改善 29.68%，相对 M07 改善 7.72%，后者独立样本差异区间跨零。
4. 另在唯一校准本地模拟器的 30 个相同场景下进行配对诊断；不称为官方成绩。选取全部场景中位顺位案例解释少走路、少测量和额外换频如何合成实际时间差。
5. 加入三次正式测验，只报告成功清除数；官方未公开实际总数，不能据此认定全清。
6. 从 15 个编号公式减至 6 个。简单坐标构造、均值、路线长度、清除阈值和统计口径改为文字；关键观测、谨慎测点、凸包覆盖、联合排除与行动代价保留。

## 复现与合并

独立编译：在本目录运行两次 `xelatex main.tex`，或运行 `tectonic main.tex`。采用与第三问一致的 Fandol 兼容字体和版式。

合入总稿：将 `chapter-resolved.tex` 放在第三问之后输入，四个图 PDF 加入图像搜索路径；需要 `needspace`、`placeins` 及原论文已有的数学、图表包。章内标签以 `q4r` 为前缀，图表和公式编号会自动接续。四条参考文献合入总参考文献，已存在的条目去重。

重绘：`python redraw_figures.py`，依赖 NumPy、SciPy、Matplotlib、Shapely。默认中文字体 SimSun；跨机若缺失，请改为已安装中文字体。SVG 保留可编辑文本，PDF 嵌入字体。

策略复验须接入项目统一入口 `contest/global/shared-src/local-simulator/run_local.py`，标识 `b-official-calibrated-20260912`。不要把代码包中的历史研究入口当模拟器。官方 API 运行需用户本地认证配置及现有受控批量脚本；本包不包含配置，不会自行启动测试。

## 证据范围

CG 是依据 Vander Hook 等 Algorithm 1 的有界域适配版，不是原论文完整 EKF 实现；beta=0.05 在任何实验前固定，未做成绩驱动调参。本批差异不代表优于原论文的所有参数或所有适配方式。

三个官方样本集合是不同案例，以独立样本统计；30 个本地场景才可配对。已测官方数据不剔除低分或失败光学操作。图示几何域为公开观测产生的估计域，清除停点不当作真实源位置；本地配对图的清除曲线延续至搜索完成，标出了任务结束。

原始官方 CG 归档：`contest/q4/exports/official-cautious-30-20260913`。三次正式测验：`contest/q4/exports/formal-m32-three-tests-20260913`。原始加密日志与带完整接口标识的动作日志保留在项目原目录；本包提供去标识数据和哈希。

全文集成版为 `../全文章节/问题四.tex`：接续全局编号，并将本节正、负观测集合记为 $\mathcal S^+$、$\mathcal S^-$，避免与前文 P、Q 重名。
