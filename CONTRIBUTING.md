# 协作规范

## 基本流程

全队按照问题一、问题二、问题三、问题四的顺序共同推进，同一时间只设置一个活跃小问。每个小问使用 `stage/qN` 作为集体集成分支，具体模型、算法、数据检查、验证和写作使用从该分支建立的短研究分支。

不为小问设置固定负责人。每项研究的作者和修改记录由 Git 提交保留。

## 研究任务

赛前可以通过 Issue 组织长期工作。比赛期间可以使用 Issue，也可以直接在 `contest/STATUS.md` 和当前小问的 `state.md` 中记录研究任务、输入、预期输出和验收条件。

短研究分支只处理一个明确问题，例如：

```text
q1/framing
q1/model-m01
q1/solve-m01
q1/verify-m01
q1/writeup
```

研究完成后，通过 Pull Request 合并到当前的 `stage/qN`。当前小问完全闭合后，再将集成分支合并到 `main`。

## 提交要求

- 提交前运行 `git status --short`，确认没有夹带其他改动。
- 使用 `git add -- <明确路径>` 暂存本次文件。
- 依次检查 `git diff --cached --check`、`git diff --cached --stat` 和完整暂存差异。
- 一个提交只记录一个可解释的进展。
- 提交信息使用简短动词，例如 `add q1 baseline model`、`verify q1 numerical result`。
- 不在 `main` 或 `stage/qN` 上直接进行未经审查的长期开发。
- 不使用强制推送改写队友已经依赖的历史。

## 建模成果验收

- 写明问题、假设、变量、目标函数或评价指标和适用边界。
- 与至少一个简单基线或可核验特例比较。
- 记录模型、算法、数据、随机种子、运行命令和 Git 提交。
- 对关键结论进行独立验证、敏感性、稳健性或误差分析。
- 被否决的方案记录原因，避免重复研究。
- 论文中的表格和图形应由代码生成，避免手工修改数字。

## 文件协作

- 本仓库不使用编辑锁，各项研究通过短分支和 Pull Request 协作。
- 原始题面和原始数据只新增，不原地修改。
- 文本冲突必须逐段核对，不能整文件覆盖。
- 二进制文件发生并行修改时保留不同版本，再明确选择或整合。
- 已被后续小问或论文引用的运行结果不得静默覆盖。

## 禁止提交

- 密钥、Cookie、登录凭证和个人身份信息。
- `.env`、个人虚拟环境、编辑器配置和操作系统缓存。
- 可以由源文件重新生成且没有复核价值的临时缓存和编译中间文件。
