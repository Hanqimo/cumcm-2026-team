# M01 模型名称

## 研究目的

说明该模型回答什么问题，以及它适用于当前小问的哪些部分。

## 数学表述

记录变量、参数、单位、目标函数或核心方程、约束、初始条件和边界条件。

## 假设与适用范围

列出题目事实、建模约定、额外假设、适用条件和潜在风险。

## 算法

记录基线算法、主算法、算法版本、选择理由、参数和停止条件。

## 目录

- `formulations/` 保存模型数学结构的实质版本。
- `src/` 保存求解代码。
- `configs/` 保存运行配置。
- `notebooks/` 保存探索性计算。
- `runs/` 保存可复现运行结果。
- `verification/` 保存独立验证和测试。

## 当前结论

说明模型目前是候选、采用、待验证还是已否决，并给出依据。
