# 本轮研究记录

2026-09-11，读取题面、附件1、附件3问题1模板和已有模型算法Tex，核对题给附录2参数。工作区初始没有待处理的Git修改，在本地研究分支 q1/solve-m01-20260911 创建本目录；所有原始输入使用独立快照，校验值保存在 inputs/manifest.json。

首先完成恒定平衡、贝塞尔解析特例、非线性制造解测试。用 N=200 的0.5/0.25秒结果及 N=200/400 的0.25秒结果复现用户的审查差异，确认粗配置不达标。N=200、0.25秒的首次附件运行核心墙钟时间约0.10秒，据此推进有界加密，每次运行预算600秒。

为减轻最初几秒瞬态对全程时间网格的计算负担，采用预先规定的六段步长，同时保留后向欧拉和Picard原方程。随后的试算表明温度主要受时间误差限制，含水率还受第1秒表面空间误差限制。利用当前两场解耦的结构，温度和含水率分别选取不同网格与步长并独立验收；各次完整结果和命令全部保留。

温度 s65536 级别的时间减半差为5.24e-6，仍未达4e-6门槛，因此继续到 s131072。最终温度取N=1600、s131072，含水率取N=12800、s8192。时间、空间差均低于门槛，观测收敛阶与方法预期一致。s表示基本步长分母，实际分段步长见README，不能将其误读成全程固定步长。

含水率容差收紧十倍后，最大结果差约6.66e-13。单次Picard拒绝测试成功拒绝未收敛候选步；独立稠密求解重现约345倍稿件非线性容差的现象，但整体收支仍很小。预期退出码3的测试完整保存，不视为失败附件运行。

核验脚本初次尝试使用SciPy时发现环境没有安装该依赖，未产生数值核验结论。随后使用NumPy稠密线性代数及显式贝塞尔幂级数作为独立实现，避免为了核验添加新依赖。没有变更主计算核心。

最终全部双精度输出、四位小数结果Excel和论文表格同源。时间与空间加密比较中仍有靠近舍入分界的末位变化，清单已保存，不把验收门槛包装为每个末位都唯一确定。工作簿两个工作表逐值核对75,600个结果单元格，四幅局部渲染检查显示标题与数字清晰、无截断。

计算机成本见各运行manifest及build_environment。此算法无随机种子；工具调用次数不用于估算AI账户额度。未执行Git暂存、提交、推送、合并，也未修改团队正式采用入口。
