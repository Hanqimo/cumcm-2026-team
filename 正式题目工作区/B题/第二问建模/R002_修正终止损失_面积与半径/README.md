# R002 修正终止损失：期望面积与期望半径

入口为 [结果汇报.md](结果汇报.md)。本轮仅求解第二问最新模型的两个期望目标，基准为第一点在原点、示向度正东。强信号损失为零，其他能够在当前点保证光学定位的反馈同样取零。

面积结果位于 `results/area/`，半径结果位于 `results/radius/`。`model/模型快照.tex` 固定本次使用的模型；`manifest.yaml` 固定环境、命令和源文件校验值。复现入口为 `src/reproduce.py`，需要 Python 3、NumPy 和 C++17 编译器。

已有旧版 R001 保留。本轮结果为本地研究候选，已完成记录范围内的数值与独立验证，尚未证明连续全局最优，未修改正式采用入口。清除概率及其改进目标留待后续研究。
