# B题第二问：最终模型与结果

本目录是本轮第二问研究的最新入口。主模型已扩展为以 \(S_1=(a,0)\) 和第一次相对示向度 \(\beta\) 为参数的一般情景，包含目标大圆裁剪、圆外第一点和观测相容性；强信号与原地光学终止损失口径保持一致。

**计算范围：现有期望目标、最坏半径与论文插图的数值结果均对应原点、正东基准 \(a=0,\beta=0\)。本次仅更新模型，没有求解一般参数情景。现有 `src/reproduce.py` 仍只复现基准结果。**

| 阅读内容 | 文件 |
|---|---|
| 模型、符号、概率更新与两个优化目标 | [第二问模型.tex](第二问模型.tex)、[第二问模型.pdf](第二问模型.pdf) |
| 数值结果与候选区域、方法和验证概要 | [结果汇报.md](结果汇报.md) |
| 假设合理性、损失口径、局限与待定方案 | [模型评估与待定方案.md](模型评估与待定方案.md) |
| 01、02论文的主要思路、对照与迁移边界 | [论文01与02阅读说明.md](论文01与02阅读说明.md) |
| 分目标机器可读结果 | [面积](results/area/result.json)、[半径](results/radius/result.json) |
| 复现入口 | [src/reproduce.py](src/reproduce.py) |
| 参数、环境与文件哈希 | [manifest.yaml](manifest.yaml) |

基准为第一点在原点、示向度正东。位置按1800米圆域面积均匀，接收半径与测角误差采用所列均匀先验并正确更新。两个目标都将距离不超过5米的强信号反馈，以及其他能够在第二点原地保证光学定位的反馈，计为零损失。

| 目标 | 第二点（米） | 期望面积损失（平方米） | 期望半径损失（米） |
|---|---|---:|---:|
| 期望面积最小 | (932.6, ±590.0) | 2103.7834 | 25.12796 |
| 期望半径最小 | (906.6, ±572.8) | 2118.1502 | 25.07572 |

两个目标分别保存在 results/area 与 results/radius，它们是同一最终模型的两个评价要求。数值为当前搜索和验证范围内的最好候选，未证明连续全局最优。清除概率目标、一般参数情景求解和分布敏感性列为待研究；原点基准的最坏情况对照见下文独立记录。

编译使用 `latexmk -xelatex -interaction=nonstopmode -halt-on-error -outdir=build 第二问模型.tex`，将 build 中的PDF复制到本目录即可更新交付PDF。中文为macOS宋体/黑体，标题不加粗，无日期，西文和公式使用Latin Modern。

求解复现需要Python 3、NumPy和C++17编译器，运行 `python3 src/reproduce.py`。本机可使用 `/Users/hanqimo/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3`。复现会覆盖当前结果，符合只保留最新结果的约定。

## 2026-09-12 补充：最坏半径备选模型

[备选模型：最坏半径](备选模型_最坏半径.md) 已完成分布已知与分布未知两种情形的数学建模，包含完整反馈区域、原地光学终止损失、严格最坏目标、候选区域及解析核查，并区分严格最坏与最坏分布下的期望。版本为 `Q2-WC-K-v01` 和 `Q2-WC-U-v01`，当前状态为建模完成、未数值求解、未正式采用；上文所列均匀期望模型及其结果继续作为基础版本保留。两种备选版本在更新后的联合相容支持相同时具有相同的严格最坏目标。

本入口不替代队友第一问资料或整篇论文草稿；其他目录的独立模型也不自动被本模型采纳。此次整理仅保存在本地仓库，未自动提交或推送。

## 2026-09-12 后续：最坏半径已求解并完成比较

无分布信息版本 `Q2-WC-U-v01` 的求解与核查已完成，见[最坏半径结果与对比](worstcase/结果与对比.md)。基准北侧点约为 (969.35, 497.73) 米，最坏半径约49.03634米；原期望最优点的最坏半径约50.37830米。改用最坏目标使最坏半径下降约2.66%，在相同均匀先验比较环境下使期望半径由25.07572米增至25.46926米，增加约1.57%。连续全域分支定界得到不足0.001米的目标值数值间隙，计算使用双精度几何余量而非定向舍入区间算术。完整坐标、37个保守1%网格候选、复现代码和验证证据独立保存在 `worstcase/`；上文“未求解”为建模阶段的历史状态，基础期望模型结果继续保留。

## 2026-09-12 论文插图

[论文插图与图注](论文插图/README.md) 提供反馈区域与最小包围圆、期望与最坏目标的空间热力图、最坏半径分支平衡三幅图，均已导出 PDF、SVG 和600 dpi PNG。对应网格数据、连续角度界、1%轮廓回代核查及复现代码独立保存在 `论文插图/`，原模型与结果文件保持不变。
