# 单一期望目标与容差候选区域历史版本

保存修改前的两份整篇论文、独立问题二、PDF 和维护记录，文件相对仓库的原路径保留于 `快照/`。该版仅优化 J，以 J <= J* + epsilon 定义候选区域，再按移动距离选择推荐点。它在 2026-09-13 被期望定位损失与完成概率加权方案替代。

此文件夹只记录历史状态，不是当前论文采用方案。逐文件 SHA-256 见 `manifest.json`。当前入口为 ../../论文定稿版/README.md。
