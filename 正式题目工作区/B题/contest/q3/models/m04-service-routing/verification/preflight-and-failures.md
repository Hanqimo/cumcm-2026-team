# 预检查和失败记录

- 初始化M04目录时，复制后裁剪`merged_cover.py`尾部的字符串定位失败：源文件使用`start`而非预期的`self.robot.position`。当时未启动模拟或生成成绩；随后基于实际源文本生成FlexibleCoverPlanner。
- R002的multicover/all_new：1150m初始外圈布局未覆盖全部保守网格，12/12均显式失败。它们的短虚拟时间全部标为无效。R003同场景修复为先验证候选布局完整覆盖、再参与优化，全部全清。原失败记录和源码快照保留。
- R009首次命令在进入main前报`ModuleNotFoundError: lookahead`，原因是将导入放在src路径设置之前；没有创建运行目录或产生模拟数据。调整导入位置后才生成R009正式记录。
- 原论文策略前瞻、提前外围探测、形心/WLS、任务合并与暖启动等都有退化场景，全部结果列入生成的trials表；不以程序正常退出代替完整性与清除证书核验。
- R012限时MILP属于开发试验；后续审计使用`--keep-actions`保存实际动作，避免将运行时间限制下的求解结果假定为跨机器逐位确定。
